#!/usr/bin/python3
"""Module that represents a pascal tirngle"""


def pascal_triangle(n):
    """Function that returns a list of lists of integers
    representing the Pascal’s triangle of n
    """
    if n <= 0:
        return []
    matrix = []
    p_row =
    for i in range(n):
        for j in range(i):
            if j == 1:
                row.append(j)
            if j and 
        matrix.append(row)
